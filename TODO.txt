## Krrish

A student claimed that "when trying to do the 10-infrastructure tutorial it requires you to make two more code spaces in the tutorial." Is that true? Is it a good idea?

Take ownership of those three tutorials (10, 11, 12). Add yourself as co-author. Aim for 60 minutes each.

It is codepsace-starter, not codebase-start.

Delete 10-infrastructure. 

Come up with a list of things we want to teach students, and then figure out where to put that stuff, and then how to do it. Examples:

1) Install VS Code extension

2) Create a repo/.devcontainer/ from scratch and start a codespace from it.

Students should know how to install a VC Code extension by hand. Maybe share an image. Then, we need to check that they did it. We almost always want this to be some command which we can run which returns evidence that the student did what we wanted. Surely there is a command which returns all the installed VS Code extensions.

Make a new devcontainer from scratch, save it as a repo, then start a codespace from it?

Do not refer to specific AIs like Claude.

No talk about templates.

Own every word. Or send me an email with your questions. But there is no reason to keep anything.

Own every knowledge drop. Several here are much less important than other stuff we should teach students.

Add some images, especially in devcontainer.

Always bold R package and Python libraries. Anything which might appear at a terminal, or a URL, is in backticks.

## Tutorial Edits

Need to ensure that, by the time we finish the core VS Code tutorials that students are very comfortable with repo, .gitginore, QMD and so on. That is, we want to guarantee that all later tutorials can start with one question that starts (and states) from the assumption that you are in a repo named `x` and which tells you to set up a QMD, .gitigore with *_files, commit/push. And then the rest of the tutorial just has the student telling AI what to do with the QMD.

But, at the same time, we don't want to overwhelm them during week 1. So, we need to bring things in slowly.

Getting Started chapter introduces Codespaces and VS Code. (Might make this longer?) But does nothing with Git/GitHub. Getting Started tutorial does a little more with Codespaces and VS Code but its main focus is showing how to work with tutorials. It does show some bash Terminal commands. Make sure to show the -h option to one command. We should have some AI questions.

We can split AI into two parts. In Getting Started and the Introduction, we just use GitHub Copilot. We know it will always be there and will always work for at least a few questions. Then, later, we can learn how to operate something like Antigravity from the command line.

There is a temptation to hold off on repos until after the first few tutorials, but then we can't publish nice pages during the quarto tutorial. So, best to just bite the bullet and have students use connect-repo early, starting in the introduction, or maybe even in Getting Started.



### 00-introduction Jacob

Skim 01-code and 02-quarto

Don't bother with Antigravity

We need a 01-introduction tutorial. 

  * Comnfortable in VS Code
  * 10 Terminal questions --- just cut and paste from Terminal
  * Run `.devcontainer/connect-repo.sh introduction`. Discuss what it does. `pwd` is a reasonable questions, as is `ls -a`. Look at the URL for your repo. (Steal from 05-github-1.)
  * Script 1 (use Copilot) -- maybe cut/paste from 01-code
  * 5  questions about Git/GitHub.
  * Log in to Antigravity (steal from 04-ai-1). Mention Copilot, and highlight the possibilities for using all sorts of AI.
  * Script 2 -- maybe cut/paste from 01-code
  * After this, we are AI agnostic. (But which pictures?)
  * Each script finishes with asking AI to make a plot using only library(tidyverse) and a the diamonds data set from the ggplot2 package.
  * Mention full access but tell students not to use it.

This would be the first tutorial that someone does after Getting Started. Indeed, it should probably repeat key issues from Getting Started. That is, it should be self-contained. We want students to start using AI within the IDE as soon as possible. So, this tutorial would include:

  * Basic vscode components, often taken from 01-code.
  * A couple of bash Terminal questions, including the -h option.
  * (maybe) make a script that adds 2 numbers. And run it. Just to show how you make a file and then do stuff with that file.
  * Two examples of using AI. 
    + The first would be GitHub Copilot, since we can be pretty sure that this will always work. Have it make a script which adds 10 numbers. Have the AI run the script. Then use show_file() so that we know that it worked.
    + The second would be Antigravity. All (?) students have Google accounts (and those that don't could just skip these questions). Once it is connected, it could make another script, and then the AI could run it. show_file() again.
  * Mention that we can't keep this code because we don't own this repo. Question could be `ls` from bash, and then discuss the files we will lose once the codebase closes.

  * Use AI --- whatever AI you want; we don't force particular AI usage --- to create a tidyverse script which makes a beautiful diamonds plot. Tell AI to run the script. show_file() to see their script.  Show/discuss our answer.
  * Git commit/push by hand. Question is `git log`.
  * Last question is URL for the repo.
  * At the end, reinforce how we always do things: Start codespace. Make repo. Start AI. Start tutorial. We assume that you understand these steps and will use them all going forward. We will just tell you to do things. We leave it to you to use AI.



### 02-code and 03-quarto 

Still good tutorials, but we make some changes. 

  * They both --- or at least 02 --- need a reminder that the student had better already be in a repo with the appropriate name and with AI of some kind ready to use.
  * You can cut some of the material from 02-code which you moved to 01. 
  * Tutorials should end with repo URL. (Maybe I should spend some time updating the claude.md where it discusses how tutorials end. Most common is with a published GitHub page. But a simple repo URL is also OK. And more advanced tutorials end with a website.)
  * We should change up the examples --- Script-1, Script-2 and so on --- so that the first version might imply that students do things by hand, just so they learn how the underlying technology works. The second and subsequent versions urge the students to use AI.
  * Right now, the examples are too much like the old world, where we add a new line to the script/quarto with each question. We don't teach coding anymore! We teach using AI and/or infrastructure.
  * Need to teach `Cmd/Ctrl + Shift + K` for rendering qmd files. Even if we use AI to do this later, it is still good to have students do it by hand a bunch, and to see the command which is issued at the bash Terminal when they use those keys.

 ### 04-terminal 
 
  * Could be shorter since students rarely use shell commands themselves. But it might also include a couple of git commands, like log and status. 

  * Could at least mention a bunch of commands, like `find`, which students will never use but which they might see AI using on their behalf.
 
  * Also, this might be a good time to explain how to Upload . . . a file. (Right click the open area in the Explorer tab and "Upload . . ." is an option. This is more important than you might think --- and we should mention these examples. You sometimes need to download data by hand, and so then need to upload it from your Downloads folder. You also sometimes need to upload an image which you want to mimic since, I think, you often can't just "paste" it in to the AI prompt.)

### 05/06 GitHub

  * Not clear that we need two tutorials.
  * We certainly don't need multiple new repos. But maybe creating one other repo, besides the one we are in, would be a good idea. But we create that one by hand! By going into the /workspace directory, looking at the two repos already there --- codebase-starter and new-repo --- and then using git commands to create a repo. 
  * The purpose of these tutorials is to teach git (and GitHub) after all.
  * Might be that the second tutorial walks through what connect-repo is doing, line by line, and issues the same or similar commands. This would show that connect-repo does more than just connect. It reloads the window . . . or whatever that is.

  ### ai tutorials

  * Maybe there aren't any AI tutorials! 00-introduction introduces the idea and then every subsequent tutorial uses AI. Indeed, the second ai tutorial (which is mostly git?) would just become part of the second GitHub tutorial.


### websites

  * It is cool for the first website tutorial to be "by hand," meaning running the `quarto create` command at the terminal, and then exploring each of the resulting files. 

  * It is cool that the website tutorials do a good job of showing students how to organize a project, with different scripts, saved data, and so on. We teach architecture, not coding.

  * Nothing wrong with having two tutorials, but probably not two "website" tutorials. Maybe the first is websites and the second is "projects" more generally, including dashboards.

  * I don't know the best way to handle (or teach) the creation of projects in the era of AI. Instead of remembering/running `quarto create`, can we just tell AI to make all the necessary files for a Quarto website? Instead of remembering `quarto publish`, can we just tell it to publish the website? 

### infrastructure

  * infrastructure is very important. I like the content which the current tutorial covers. Students need to understand devcontainers. 

  * Need to end infrastructure tutorial (or add another tutorial) with a topic for creating your own `codespace-starter`, maybe called `starter`.

  * But surely there is more infrasture-related topics to cover. Most important might be databases. How about working with APIs? Grabbing live data? 




### Other Stuff

Tutorial times from 2026

   source                    minutes     N
   <fct>                       <dbl> <int>
 1 getting-started_answers      35      47
 2 introduction-to-r_answers    45      43
 3 01-code_answers              85      40
 4 02-quarto_answers            90      35
 5 03-terminal_answers         120      32
 6 04-ai-1_answers              78      28
 7 05-github-1_answers          60      29
 8 06-github-2_answers          73      28
 9 07-ai-2_answers              50      27
10 r4ds-1_answers               90      22
11 08-websites-1_answers        60      24
12 r4ds-2_answers               90      15

Update images in 01-code.

Should longer tutorials like 1, 4 and 7 have one of their exercises removed?

* Finish skeleton in tutorial.helpers

* Finish "Tutorials in the Age of AI" in tutorial.helpers.

* Rewrite "Instructions"

* Do we need an AI Getting Started tutorial which would highlight things like don't use install.packages() in your scripts and other generic lessons for working with AI? Maybe this is an "Introduction to AI" tutorial in tutorial.helpers?


* Keep package help page and README page in sync better.

* What do we do with skeleton.Rmd in tutorial.helpers package?


* Add how to delete a repo, and then delete one of the ones you made.

For when we revisit the details of the terminal: https://code.visualstudio.com/docs/terminal/basics

## Things to do earlier

In total, we make 5 websites. We need to make their construction more consistent, making things simpler in the initial ones and more complex, with more skipped steps, in later ones. Students will probably be doing all these within a day or two.

## Other items

To shrink the size of all the PNG files, do:

find . -type f -name "*.png" -exec pngquant "{}" -o "{}" --force \;

We could do all the example repos in PPBDS and then leave them up forever, so they would be available to students doing the work. Or for a ppbds-student account.
